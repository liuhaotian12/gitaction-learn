name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting 
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Done -bye!"
      - name: Print Multiple
        run: |
          echo "this is first line"
          echo "this is seccond line"
          echo " this is third line"
